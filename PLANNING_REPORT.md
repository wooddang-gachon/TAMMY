# 프로젝트 기획 보고서 — AI 서버

> 웰니스 펫 에이전트 **타미(Tammy)** 의 AI 서버(`tammy-ai-server`) 관점에서 작성했습니다.
> 서비스 서버·클라이언트 영역(Ⅰ, Ⅴ)은 이 문서의 범위에서 제외했습니다.

---

## Ⅰ. 문제 정의 및 사용자 분석

*(서비스 기획 문서에서 다룹니다. AI 서버 범위 밖)*

---

## Ⅱ. 기술 스택 선정

### 1. 기술 스택 요약

| 영역 | 기술 / 라이브러리 | 선정 사유 |
| :--- | :--- | :--- |
| **Backend** | Go 1.25 · Gin | 요청 대부분이 모델 응답 대기(I/O)라 고루틴 동시성이 잘 맞고, 정적 바이너리라 콜드 스타트가 짧습니다 |
| **Database** | 없음 (stateless) | 대화 맥락·기록은 매 요청마다 서비스 서버가 전달합니다. 저장소가 없어 인스턴스를 그대로 늘릴 수 있습니다 |
| **AI / ML** | Genkit for Go · Gemini 3.5 Flash Lite · dotprompt | 프롬프트·하이퍼파라미터·출력 스키마를 `.prompt` 파일 한 곳에 모으고, 구조화 출력을 모델 단에서 강제합니다 |
| **Infra & DevOps** | Cloud Run (asia-northeast3) · Artifact Registry · Secret Manager · distroless 이미지 | 트래픽이 없을 때 0으로 축소되고, 시크릿을 이미지에 넣지 않고 주입할 수 있습니다 |

### 2. 세부 선정 근거

- **Go / Gin**: 프롬프트를 `//go:embed`로 바이너리에 포함해 배포물이 정적 바이너리 하나(distroless, non-root, 약 57MB)입니다. 컨테이너에 프롬프트 파일을 따로 실을 필요가 없습니다.
- **Genkit + dotprompt**: `temperature`, `topP`, `maxOutputTokens`, 출력 스키마를 프롬프트 파일 frontmatter에 선언합니다. 프롬프트 수정이 코드 수정과 분리되고, 기동 시점에 전체 프롬프트가 검증됩니다.
- **Gemini 3.5 Flash Lite**: 이미지 인식, 검색 그라운딩, 긴 리포트 입력을 한 모델로 처리하면서 응답이 2~9초 수준입니다. 엔드포인트별로 `VISION_MODEL` / `CHAT_MODEL` / `REPORT_MODEL` / `RESEARCH_MODEL` 환경 변수를 두어 필요한 기능만 상위 모델로 교체할 수 있게 했습니다.

---

## Ⅲ. 요구사항 분석

### 1. 기능적 요구사항 (Functional Requirements)

- **[FR-01] 음식 이미지 인식** — 사진에서 음식을 모두 찾아 이름·바운딩 박스·확신도를 반환합니다. 우선순위: **High**
- **[FR-02] 영양 정보 조회** — 음식명 배열을 받아 웹 검색으로 확인한 영양 수치와 출처를 반환합니다. 우선순위: **High**
- **[FR-03] 대화 및 감정 분석** — 타미 페르소나로 답변하고, 같은 호출에서 사용자 발화의 감정과 반응 모션을 함께 반환합니다. 우선순위: **High**
- **[FR-04] 웰니스 리포트 5종** — 식습관·마음챙김·생활습관·수분·장기 회고 리포트를 마크다운으로 생성합니다. 우선순위: **High** (장기 회고만 Medium)
- **[FR-05] 운영 인터페이스** — 인증 없는 헬스 체크와 API 키 뒤에 놓인 Swagger 문서를 제공합니다. 우선순위: **Medium**

### 2. 비기능적 요구사항 (Non-Functional Requirements)

- **성능 (Performance)**: 실측 기준 대화 약 2초, 음식 인식 5~8초, 영양 조회 약 9초(모델 2회 호출), 리포트 3~6초입니다. 요청 타임아웃은 120초, Cloud Run 타임아웃은 300초입니다.
- **확장성 (Scalability)**: 상태를 저장하지 않아 인스턴스가 서로 완전히 대체 가능합니다. 인스턴스당 동시 요청 40으로 두고 수평 확장합니다.
- **안정성/가용성 (Reliability)**: 기동 시 프롬프트 9종을 전수 검증해 잘못된 템플릿은 배포 단계에서 실패시킵니다. SIGTERM 수신 시 30초간 진행 중인 요청을 흘려보내고, 패닉은 500 응답으로 복구합니다. 모델 오류(502)와 지연(504)을 서로 다른 코드로 구분해 서비스 서버가 재시도 여부를 판단할 수 있게 했습니다.
- **보안 및 규정 준수 (Security & Compliance)**: 내부 API 키가 비어 있으면 서버가 아예 기동하지 않습니다. 감정일기 본문 등 민감 입력은 로그에 남기지 않습니다.

---

## Ⅳ. 서비스 설계

### 1. 시스템 아키텍처 (System Architecture)

![AI 서버 아키텍처](docs/images/ai-architecture.png)

- 클라이언트는 AI 서버를 직접 호출하지 않습니다. 서비스 서버만 `X-Internal-Api-Key` 헤더를 붙여 내부 호출합니다.
- AI 서버는 DB에 접근하지 않습니다. 필요한 데이터는 전부 요청 본문으로 받습니다.
- 프롬프트 템플릿은 바이너리에 임베드되어, 배포 아티팩트가 바이너리 하나로 끝납니다.

### 2. 핵심 사용자 흐름 (User Flow)

식사 기록 흐름은 **인식과 영양 조회를 두 단계로 분리**한 것이 핵심입니다.

![음식 인식 · 영양 조회 파이프라인](docs/images/ai-food-pipeline.png)

- 사진 한 장에 여러 음식이 담기는 것이 일반적이므로 인식 결과를 배열로 돌려줍니다.
- 사용자가 "공기밥"을 "현미밥"으로 고친 뒤 조회해야 영양 수치가 정확해지므로, 조회를 별도 엔드포인트로 뺐습니다.

### 3. 주요 비즈니스 로직 및 정책

| 정책 | 내용 |
| :--- | :--- |
| 바운딩 박스 변환 | 모델에서는 Gemini 네이티브 형식(`[ymin, xmin, ymax, xmax]`, 0~1000)으로 받고, 서버가 0.0~1.0 정규화 좌표로 변환·클램프합니다. 모델에 직접 정규화 좌표를 요구하면 좌표 품질이 크게 떨어집니다 |
| 감정 값 보정 | `state`가 서비스 서버 enum 밖이면 `CALM`으로, `motionType`이 목록 밖이면 감정별 기본 모션으로 보정합니다. 응답을 그대로 `emotion_logs`에 기록해도 안전합니다 |
| 영양 응답 정합 | 요청한 음식명과 **같은 순서, 같은 개수**로 맞춥니다. 검색으로 확인하지 못한 음식은 수치 `0`, `confidence` `0`으로 채웁니다 |
| 출처 검증 | 절대 URL로 파싱되지 않는 출처는 버립니다. 모델이 지어낸 참조를 막는 가장 싼 방어입니다 |
| 인식 실패 일관성 | 모델이 성공을 주장하면서 빈 배열을 준 경우 `isIdentified`를 `false`로 되돌려 서비스 서버 폴백이 정상 동작하게 합니다 |
| 컨텍스트 상한 | 대화는 최근 30턴, 장기 회고는 최근 400턴까지만 모델에 전달합니다 |
| 기록 밀도 | `dataDensity`(thin/normal/rich)를 받아 톤만 조정합니다. 생략하면 해당 지침을 렌더하지 않아 프롬프트가 길어지지 않습니다 |
| 민감 정보 | 감정일기 본문은 로그에 남기지 않고, `diaryId`는 모델에 전달하지 않아 리포트 본문으로 식별자가 새어나갈 수 없습니다 |

---

## Ⅴ. 화면 설계

*(클라이언트 기획 문서에서 다룹니다. AI 서버 범위 밖)*

---

## Ⅵ. 데이터 및 API 설계

### 1. 데이터 모델링

AI 서버는 저장소를 갖지 않습니다. 영속 모델 대신 **DTO가 서비스 서버와의 공개 계약**이며, 감정 값(`HAPPY`/`SAD`/`ANGRY`/`STRESSED`/`CALM`)은 서비스 서버 Prisma `EmotionState` enum과 정확히 일치시켰습니다.

### 2. 핵심 API 명세

모든 `/v1` 엔드포인트는 `X-Internal-Api-Key` 헤더를 요구합니다.

| Method | Path | 설명 |
| :--- | :--- | :--- |
| POST | `/v1/vision/analyze-food` | 이미지에서 음식 인식 + 바운딩 박스 |
| POST | `/v1/nutrition/lookup` | 음식명 배열 → 영양 정보 + 출처 (웹 검색) |
| POST | `/v1/chat/process` | 타미 대화 + 사용자 감정 분석 |
| POST | `/v1/reports/diet` \| `/mindfulness` \| `/lifestyle` \| `/hydration` \| `/retrospective` | 리포트 5종 (`title` / `markdown` / `nextActionChecks` 공통 응답) |
| GET | `/health` | 헬스 체크 (인증 불필요) |

에러는 전부 `{ "code": ..., "message": ... }` 한 가지 형태입니다. `code`는 서비스 서버의 폴백 동작과 연결됩니다.

| Code | Status | Code | Status |
| :--- | :--- | :--- | :--- |
| `INVALID_REQUEST` | 400 | `UNSUPPORTED_IMAGE_TYPE` | 415 |
| `IMAGE_REQUIRED` | 400 | `AI_MODEL_ERROR` | 502 |
| `UNAUTHORIZED` | 401 | `IMAGE_FETCH_FAILED` | 502 |
| `IMAGE_TOO_LARGE` | 413 | `AI_MODEL_TIMEOUT` | 504 |

---

## Ⅶ. AI 기능 설계

### 1. AI 기능 정의 및 목적

네 갈래로 나뉩니다.

| 기능 | 해결 과제 |
| :--- | :--- |
| 비전 | 사진 한 장에서 여러 음식을 분리해 인식하고, 화면이 표시할 위치까지 함께 제공합니다 |
| 영양 리서치 | 모델 기억이 아니라 **검색으로 확인한** 영양 수치와 출처를 제공합니다 |
| 대화 | 공감형 페르소나 응답과 사용자 감정 분석을 한 번의 호출로 처리합니다 |
| 리포트 | 집계 수치가 아니라 원시 로그를 받아, 그 기록이 사용자에게 어떤 의미인지를 씁니다 |

### 2. AI 모델 및 파이프라인 설계

**사용 모델**: Gemini 3.5 Flash Lite 단일 모델을 쓰되, 용도별 환경 변수로 개별 교체할 수 있습니다.

**프롬프트 설계**: dotprompt 9개와 공용 partial 2개(`_persona`, `_report_rules`)로 구성했습니다. 페르소나와 리포트 규칙을 partial로 분리해 대화와 리포트 5종이 같은 말투를 공유합니다. 하이퍼파라미터는 작업 성격에 맞춰 다르게 두었습니다.

| 프롬프트 | temperature | 의도 |
| :--- | :--- | :--- |
| `chat` | 0.85 | 대화는 매번 다르게 반응해야 합니다 |
| `report_*` | 0.7 | 따뜻한 서술은 유지하되 규칙은 지키게 합니다 |
| `vision_food` | 0.2 | 좌표와 음식명은 흔들리면 안 됩니다 |
| `nutrition_research` | 0.1 | 검색 결과에 충실해야 합니다 |
| `nutrition_structure` | 0 | 파싱은 결정적이어야 합니다 |

**컨텍스트 관리**: 서버가 상태를 갖지 않으므로 맥락은 매 요청 입력으로 받고, 대화 30턴·회고 400턴으로 상한을 둡니다. 날짜별 원시 로그는 Go 렌더러가 사람이 읽는 텍스트로 변환해 넘깁니다. 중첩 JSON을 그대로 넣는 것보다 요약 품질이 안정적이었습니다.

**Tool Calling / 그라운딩**: 영양 조회는 모델을 두 번 호출합니다. 검색 툴과 JSON 스키마를 한 호출에서 함께 쓸 수 없기 때문에, 1차에서 `googleSearch`로 그라운딩된 자유 텍스트를 얻고 2차에서 스키마에 맞춰 구조화합니다. 그 대가로 이 엔드포인트만 응답이 느립니다(약 9초).

### 3. 예외 및 Fallback 전략

| 위험 | 대책 |
| :--- | :--- |
| 스키마 이탈 | 출력 스키마를 모델 단에서 강제하고, 디코드 실패는 502로 변환합니다. 이때 응답 본문은 로그에 남기지 않고 프롬프트명과 길이만 기록합니다 |
| 출력 잘림 | 반찬이 많은 상차림은 항목이 40개를 넘겨 JSON이 잘리므로, 비전 프롬프트의 `maxOutputTokens`를 8192로 잡았습니다 |
| 환각 | 출처 URL 검증, 요청 순서 정렬, enum 보정, 미확인 항목의 `confidence` 0 처리로 걸러냅니다. 낮은 확신도는 서비스 서버가 직접 입력을 유도하는 신호로 씁니다 |
| 응답 지연 | 타임아웃을 `AI_MODEL_TIMEOUT`(504)으로 별도 구분해 재시도 대상임을 알립니다. 장기 회고 리포트는 비동기 호출을 권장합니다 |
| 인식 실패 | `isIdentified: false` + 빈 배열로 내려 클라이언트의 재촬영·수동 입력 폴백으로 넘깁니다 |
| 안전성 | 페르소나 단에서 의학적 진단, 타 사용자와의 비교, 명령형 표현, 칼로리로 압박하기를 금지하고, 위험 신호가 반복되면 전문가의 도움을 권하도록 했습니다 |

---

## Ⅷ. 개발 환경 및 협업 방식

### 1. 개발 환경 설정

`.env`에 `GEMINI_API_KEY`만 채우면 `make run`으로 로컬 실행되고, `http://localhost:8000/swagger/index.html`에서 전체 스키마를 확인할 수 있습니다. 로컬 `debug` 모드에서는 내부 API 키 없이도 기동합니다.

| 명령 | 용도 |
| :--- | :--- |
| `make run` / `make test` / `make lint` | 실행 / 테스트(race 포함) / `gofmt`+`go vet` |
| `make docs` / `make docker` / `make deploy` | Swagger 재생성 / 이미지 빌드 / Cloud Run 배포 |

### 2. 협업 규칙 및 컨벤션

- AI 서버는 별도 레포로 분리하고, 서비스 서버와는 REST 계약으로만 붙습니다.
- 계약이 바뀌면 `SERVICE_INTEGRATION.md`에 변경 요청서 형태로 정리해 전달합니다(요청·응답 diff, 필요한 작업, 체크리스트 포함).
- 프롬프트를 수정하면 `go test ./internal/ai/ -run TestPrompts`로 frontmatter 오류와 partial 누락을 먼저 잡습니다. 현재 전체 테스트는 통과 상태입니다.
- `docs/`는 Swagger 생성물이므로 직접 수정하지 않습니다.

---

## Ⅸ. 보안 및 배포 계획

### 1. 보안 및 개인정보 보호

- **인증**: Cloud Run을 `--allow-unauthenticated`로 배포하는 대신 `X-Internal-Api-Key`가 유일한 접근 제어 수단입니다. 키를 상수 시간 비교로 검증하고, Swagger 문서도 같은 키 뒤에 둡니다. 키가 비어 있으면 `release` 모드에서 기동을 거부합니다.
- **시크릿**: `GEMINI_API_KEY`, `INTERNAL_API_KEY`를 Secret Manager에서 주입합니다. `.env`는 로컬 편의용이며 실제 환경 변수가 항상 우선합니다.
- **개인정보**: 감정일기 본문과 모델 출력 본문은 로그에 남기지 않습니다. 요청 로그는 method·path·status·소요시간만 기록합니다.
- **입력 검증**: 이미지는 매직 바이트로 형식을 판별해 JPEG/PNG/WebP/HEIC만 허용하고, 8MiB를 넘으면 413으로 거절합니다. `imageUrl` 다운로드에는 15초 타임아웃과 크기 상한을 겁니다.

### 2. CI/CD 및 배포 계획

`make deploy` 한 번으로 Cloud Run에 배포합니다(메모리 512Mi, 타임아웃 300초, 동시성 40). 런타임 이미지는 distroless·non-root 정적 바이너리입니다.

### 3. 모니터링 및 운영 계획

- 로그는 전부 JSON 구조화 출력이라 Cloud Logging에서 그대로 필드 검색이 가능합니다. 클라이언트 실수는 warn, 상위 시스템 실패는 error로 레벨을 나눠 알림 대상을 구분합니다.
- 외부 모니터링은 **`/health`** 를 바라봐야 합니다. `/healthz`는 Cloud Run에서 Google 프론트엔드에 가로막혀 컨테이너까지 오지 않습니다.
- 헬스 체크 로그는 제외해 로그를 잠식하지 않게 했습니다.

---

## Ⅹ. 남은 과제

| 항목 | 상태 |
| :--- | :--- |
| `motionType` 6종 허용 목록 | AI 서버 측 제안. 클라이언트 애니메이션 세트와 맞춘 뒤 확정이 필요합니다 |
| 서비스 서버 리포트 입력 | 집계 스칼라(일부 하드코딩 더미값) → 날짜별 원시 로그 조회 쿼리로 교체가 필요합니다 |
| 장기 회고 리포트 | 서비스 서버의 `setTimeout` 기반 가짜 큐를 실제 비동기 처리(작업 큐 + 상태 저장)로 교체해야 합니다 |
| 영양 조회 캐싱 | 웹 검색 때문에 느리므로 서비스 서버 `foods` 테이블 캐싱을 권장합니다 |
