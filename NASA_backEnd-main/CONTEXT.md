# TAMMY Star Travel Domain Context & Glossary

## 1. Core Concept (핵심 개념)
TAMMY Star Travel은 사용자의 일상 웰니스 기록(식사, 물, 감정, 운동)을 "타미와 함께 우주를 탐험하는 여행 서사"로 치환하는 게이미피케이션 시스템이다.

## 2. Two-Gauge System (두 개의 게이지)
- **전역 연료 (Fuel)**: 0 ~ 100 범위의 단일 공용 잔액. 어떤 웰니스 활동이든 1회 기록 시 +10씩 충전되며, 100에서 상한(클램프)된다. 출발(Depart) 시 100을 즉시 소모하여 0으로 차감된다.
- **행성별 남은 거리 (Distance)**: 100 ~ 0 범위의 행성별 독립 지표. 해당 행성 영역의 웰니스 활동을 실천할 때마다 거리가 -5 또는 -10씩 좁혀진다. 0에 도달하면 도착 가능(Ready to Depart) 상태가 되며, 도착(Arrive) 시 해당 행성만 100으로 리셋된다.

## 3. Five Planets (5대 행성)
1. **식사별 (`meal`)**: 식단 사진 분석 및 식사 확정 기록 (1회당 거리 -10, Fuel +10). 10회 실천 시 거리 0 도달.
2. **수분별 (`water`)**: 1-Tap 물 섭취 기록 (1회당 거리 -5, Fuel +10). 20회 실천 시 거리 0 도달.
3. **감정별 (`emotion`)**: 퀵버튼 감정 기록(거리 -5), AI 채팅 대화(거리 -5), 감정일기 작성(거리 -10). 10~20회 실천 시 거리 0 도달.
4. **생활습관별 (`habit`)**: 운동/산책/스트레칭 기록 (1회당 거리 -10, Fuel +10). 10회 실천 시 거리 0 도달.
5. **회고별 (`retro`)**: 사용자가 직접 기록하지 않으며, 매월 1일 09:00 KST에 지난 한 달 데이터를 종합 집계하여 웰니스 스코어와 함께 자동 발행.

## 4. Lifecycle States (행성 상태 전이)
- **`READY`**: 일상 기록을 통해 거리 감소 및 Fuel 충전 중인 상태.
- **`TRAVELING`**: `Fuel == 100` 및 `Distance == 0` 상태에서 사용자가 출발(`depart`) 버튼을 눌러 워프 애니메이션 중인 상태. (이 상태 중에 남긴 일상 기록의 Fuel은 새 사이클 연료로 정상 적립됨)
- **`ARRIVED`**: 애니메이션 완료 후 도착(`arrive`) 처리가 호출되어 해당 행성 거리가 100으로 리셋되고, AI 리포트 생성이 비동기 트리거되는 상태.

## 5. Idempotency & Auditing (멱등성 및 감사)
- **`clientRequestId`**: 모든 기록 API에 필수/선택 전달되는 클라이언트 고유 식별자. 네트워크 재전송 시 게이지 중복 변동 없이 기존 결과 반환.
- **`fuel_logs`**: 연료 적립/소모 내역을 추적하는 감사 로그 테이블.
