# AI 서버(tammy-ai-server)와 공유하는 내부 API 키.
# 모든 /v1/* 요청에 X-Internal-Api-Key 헤더로 실려 나갑니다.
# AI 서버가 Gemini 키를 자체 관리하므로 모델 제공자 키는 여기 필요 없습니다.
AI_INTERNAL_API_KEY="your-internal-api-key-here"



# This was inserted by `prisma init`:
# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

DATABASE_URL="" # 비밀번호 속 골뱅이(@) 특수문자는 URL 구분자 오인식을 방지하고자 %40으로 인코딩됨
MOCK_DATABASE_URL="" # Jest 등 테스트 모드(NODE_ENV=test) 작동 시 자동으로 스위칭될 가상 테스트 DB 주소

AI_SERVER_API="" # 서버 API 주소
AI_API=""

# JWT 인증 Secret Key (프로덕션 환경에서는 반드시 강력한 랜덤 키를 설정하세요)
JWT_SECRET=""

# Swagger UI 접근 인증 (프로덕션 환경에서는 반드시 변경하세요)
SWAGGER_USER="admin"
SWAGGER_PASSWORD=""
# AI Server URL
AI_SERVER_URL="https://tammy-ai-server-601688473805.asia-northeast3.run.app"
